# mix
amp - amplify wave thread from standard input
## Installation
To install run:
``
	$ mk install
``
By default installtion happens into "$HOME/local/bin". Can be changed in "mkconfig" file'
Right now works just with signed float(IEEE float).
